[build-system]
requires = [
    "setuptools ~= 82.0.1",
    "wheel ~= 0.47.0",
    "scikit-build ~= 0.19.0",
    "cmake ~= 4.3.2",
    "ninja ~= 1.13.0",
]
build-backend = "setuptools.build_meta"

[flake8]
exclude = [
    "hex",
    "__pycache__",
    "lib"
]
